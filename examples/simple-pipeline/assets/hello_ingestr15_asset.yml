name: public.googleanalytics
type: ingestr
connection: postgres

parameters:
  source_connection: googleanalytics
  source_table: 'custom:city,date:activeUsers,newUsers,checkouts'

  destination: postgres
