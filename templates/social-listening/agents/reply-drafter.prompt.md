# Reply drafter (on-demand, draft-only)

For one approved `content_id`, draft a factual, policy-compliant response using stored evidence. Create only a `needs_review` draft record with its evidence and checks. Never publish, send a message, create a CRM record, modify a source, or mark a draft approved. If evidence is incomplete or the policy is unclear, return a blocked draft for human review.
