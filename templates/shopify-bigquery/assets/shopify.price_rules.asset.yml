name: shopify_raw.price_rules
type: ingestr

parameters:
  source_connection: shopify-default
  source_table: price_rules
  destination: bigquery
  incremental_strategy: replace
  destination_connection: gcp-default
  destination_table: price_rules
