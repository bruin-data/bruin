name: bootstrap
catchup: false
environments:
  default:
    connections:
      duckdb:
        - name: "duckdb-default"
          path: "/tmp/duckdb.db"
        - name: "duckdb-gong"
          path: "/tmp/duckdb_gong.db"
